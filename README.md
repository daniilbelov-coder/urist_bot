# 🤖 Бот для генерации дисклеймеров Яндекс Лавки

Telegram-бот для автоматической генерации юридически корректных дисклеймеров для рекламных креативов Яндекс Лавки.

## 📋 Возможности

- **7 типов креативов**: Скидка новичка (динамическая/классическая), промокод, сертификат, имиджевый, продуктовый, вендорский
- **Пошаговое создание** с интуитивным интерфейсом
- **Автоматическая валидация** всех параметров
- **Поддержка всех городов** присутствия Яндекс Лавки (корпоративные и франшизные)
- **Два канала размещения**: ТВ/Радио (полная версия юр.лица) и другие форматы (короткая версия)

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонируйте репозиторий**
```bash
git clone <your-repo-url>
cd urist_bot
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`**
```bash
cp .env.example .env
```

5. **Получите токен бота**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot` и следуйте инструкциям
   - Скопируйте токен и добавьте в `.env`:
   ```
   BOT_TOKEN=your_bot_token_here
   ```

6. **Запустите бота**
```bash
python bot.py
```

## 🚂 Деплой на Railway

### Подготовка

1. **Создайте аккаунт на [Railway](https://railway.app/)**

2. **Установите Railway CLI** (опционально)
```bash
npm i -g @railway/cli
```

### Деплой через веб-интерфейс

1. Авторизуйтесь на Railway
2. Нажмите **"New Project" → "Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит настройки из `Procfile` и `runtime.txt`

### Настройка переменных окружения

В настройках проекта на Railway добавьте переменные:

- **BOT_TOKEN** — токен вашего бота из @BotFather
- **LOG_LEVEL** (опционально) — уровень логирования (DEBUG, INFO, WARNING, ERROR)

### Деплой через CLI

```bash
# Войдите в Railway
railway login

# Инициализируйте проект
railway init

# Добавьте переменные окружения
railway variables set BOT_TOKEN=your_bot_token_here

# Задеплойте
railway up
```

## 📁 Структура проекта

```
urist_bot/
├── bot.py                 # Основной файл бота
├── config.py              # Конфигурация и переменные окружения
├── models.py              # Модели данных (города, юр.лица, типы креативов)
├── generator.py           # Генератор дисклеймеров с валидацией
├── handlers.py            # Обработчики команд и сообщений
├── states.py              # FSM состояния для пошагового создания
├── keyboards.py           # Клавиатуры для интерфейса
├── requirements.txt       # Python зависимости
├── Procfile              # Конфигурация для Railway
├── runtime.txt           # Версия Python
├── .env.example          # Пример файла окружения
├── .gitignore            # Git ignore файл
├── disclaimer_rules.md   # Полная спецификация правил
└── README.md             # Этот файл
```

## 💬 Использование бота

### Команды

- `/start` — Начать работу с ботом
- `/create` — Пошаговое создание дисклеймера
- `/help` — Справка по работе с ботом

### Пошаговое создание

1. **Выберите тип креатива** из списка
2. **Выберите географию** (МО или конкретный город)
3. **Выберите канал размещения** (ТВ/Радио или другие форматы)
4. **Введите параметры** в зависимости от типа креатива:
   - Даты (формат: ДД.ММ.ГГ)
   - Размер скидки
   - Условия применения
   - И другие специфичные параметры
5. **Проверьте данные** и подтвердите генерацию
6. **Скопируйте готовый дисклеймер**

### Примеры типов креативов

#### Скидка новичка (динамическая)
Для МО, с автоматической ссылкой `clck.ru/397gmg`

#### Скидка новичка (классическая)
Для отдельных городов, с максимальным размером скидки и опциональной информацией о доставке

#### Промокод
С гибкими условиями: процент/рубли, минимальная сумма заказа, максимальная скидка, для первого заказа или конкретной категории

#### Сертификат
С количеством применений и ссылкой на условия

#### Имиджевый/Продуктовый/Вендорский
Упрощенные варианты с минимальными параметрами

## 🏙 Поддерживаемые города

### Корпоративные города (используют юр.лицо Яндекс Лавки)
- Москва + МО
- Санкт-Петербург + ЛО
- Казань
- Новосибирск
- Нижний Новгород
- Ростов
- Краснодар
- Екатеринбург
- Челябинск
- Тюмень
- Сочи
- Воронеж
- Пермь

### Франшизные города (используют свое юр.лицо)
- Тула
- Тверь
- Ярославль
- Рязань
- Калуга
- Великий Новгород
- Обнинск
- Липецк
- Иваново
- Тамбов
- Владимир
- Иркутск

## 🔧 Технические детали

### Технологии

- **Python 3.11**
- **aiogram 3.4** — современный фреймворк для Telegram ботов
- **FSM (Finite State Machine)** — для управления состояниями диалога
- **Pydantic** — для валидации данных
- **python-dotenv** — для управления переменными окружения

### Валидация

Бот проверяет:
- ✅ Формат дат (ДД.ММ.ГГ)
- ✅ Наличие города в списке доступных
- ✅ Положительные числовые значения
- ✅ Процентные значения (0-100)
- ✅ Логический порядок дат (начало < конец)
- ✅ Обязательные поля для каждого типа креатива

### Генерация дисклеймера

Процесс генерации:
1. Валидация всех параметров
2. Определение версии юр.лица (короткая/полная) на основе канала
3. Выбор юр.лица на основе географии (корпоративное/франшизное)
4. Заполнение шаблона с учетом всех условий
5. Вставка автоматических элементов (ссылки, стандартные тексты)
6. Финальная проверка и форматирование

## 📝 Логирование

Бот логирует:
- Запуск и остановку
- Обработку команд
- Ошибки валидации
- Технические ошибки

Уровень логирования настраивается через переменную `LOG_LEVEL` в `.env`.

## 🔒 Безопасность

- ❌ Не коммитьте `.env` файл в Git
- ✅ Используйте `.env.example` как шаблон
- ✅ Храните токены в переменных окружения Railway
- ✅ Регулярно обновляйте зависимости

## 🐛 Отладка

### Локальная отладка

```bash
# Установите LOG_LEVEL=DEBUG в .env
LOG_LEVEL=DEBUG

# Запустите бота
python bot.py
```

### Отладка на Railway

```bash
# Просмотр логов
railway logs

# Подключение к shell
railway shell
```

## 📚 Документация

Полная спецификация правил генерации дисклеймеров находится в файле `disclaimer_rules.md`.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи бота
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что токен бота валиден
4. Убедитесь, что используется Python 3.11+

## 📄 Лицензия

MIT License

## 🎯 TODO

- [ ] Добавить распознавание запросов в свободной форме
- [ ] Добавить кнопку "Экспорт в файл"
- [ ] Добавить историю созданных дисклеймеров
- [ ] Добавить админ-панель для управления городами
- [ ] Интеграция с базой данных для статистики

---

Made with ❤️ for Yandex Lavka
