# 🚀 Инструкция по деплою на Railway

## Шаг 1: Подготовка репозитория на GitHub

### Локально
```bash
cd /Users/daniil-belov/Desktop/Cursor/urist_bot

# Инициализируйте git (если еще не сделано)
git init

# Добавьте файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Yandex Lavka disclaimer bot"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/your-username/your-repo-name.git

# Запушьте код
git branch -M main
git push -u origin main
```

## Шаг 2: Получите токен бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `Yandex Lavka Disclaimer Bot`)
   - Введите username бота (например: `lavka_disclaimer_bot`)
4. Скопируйте токен (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 3: Деплой на Railway

### Через веб-интерфейс

1. Перейдите на [railway.app](https://railway.app/)
2. Нажмите **"Start a New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйтесь через GitHub
5. Выберите репозиторий `urist_bot`
6. Railway автоматически определит Python проект

### Настройка переменных окружения

1. Откройте ваш проект на Railway
2. Перейдите в **Variables**
3. Добавьте переменную:
   - **Key**: `BOT_TOKEN`
   - **Value**: `ваш_токен_от_botfather`

4. (Опционально) Добавьте:
   - **Key**: `LOG_LEVEL`
   - **Value**: `INFO`

### Запуск

1. Railway автоматически запустит деплой
2. Проверьте логи в разделе **Deployments**
3. Если все ОК, увидите: `Starting bot...`

## Шаг 4: Проверка работы

1. Найдите своего бота в Telegram по username
2. Отправьте `/start`
3. Вы должны получить приветственное сообщение
4. Попробуйте команду `/create`

## 🔧 Управление на Railway

### Просмотр логов
```bash
# Через CLI
railway logs

# Или в веб-интерфейсе:
# Projects → Your Project → Deployments → View Logs
```

### Перезапуск бота
```bash
railway restart
```

### Обновление кода
```bash
# Локально внесите изменения
git add .
git commit -m "Update bot"
git push

# Railway автоматически задеплоит новую версию
```

### Остановка бота
В веб-интерфейсе Railway:
Settings → Danger Zone → Pause Service

## ⚙️ Альтернативный деплой через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Свяжите проект
railway link

# Установите переменные
railway variables set BOT_TOKEN="ваш_токен"

# Задеплойте
railway up

# Просмотр логов
railway logs
```

## 🐛 Решение проблем

### Бот не отвечает
1. Проверьте логи: `railway logs`
2. Убедитесь, что `BOT_TOKEN` установлен правильно
3. Проверьте статус деплоя в веб-интерфейсе

### Ошибка "BOT_TOKEN is not set"
1. Перейдите в Variables на Railway
2. Убедитесь, что `BOT_TOKEN` добавлен
3. Перезапустите сервис

### Ошибка импорта модулей
1. Проверьте `requirements.txt`
2. Убедитесь, что все зависимости указаны
3. Пересоберите проект

### Бот падает с ошибкой
1. Установите `LOG_LEVEL=DEBUG`
2. Проверьте логи для детальной информации
3. Исправьте ошибку и запушьте обновление

## 📊 Мониторинг

Railway предоставляет:
- 📈 Метрики использования CPU и памяти
- 📝 Логи в реальном времени
- 🔔 Алерты при падении сервиса

## 💰 Стоимость

Railway предоставляет:
- **$5 бесплатно каждый месяц** (достаточно для небольшого бота)
- Далее: **$0.000463 за CPU-секунду** и **$0.000231 за GB-секунду RAM**

Для оптимизации:
- Бот использует минимум ресурсов
- Работает только когда получает сообщения
- Нет базы данных (только memory storage)

## ✅ Чеклист деплоя

- [ ] Создан репозиторий на GitHub
- [ ] Код запушен в main ветку
- [ ] Получен токен от BotFather
- [ ] Создан проект на Railway
- [ ] Подключен GitHub репозиторий
- [ ] Установлена переменная BOT_TOKEN
- [ ] Деплой успешно завершен
- [ ] Логи показывают "Starting bot..."
- [ ] Бот отвечает на /start в Telegram
- [ ] Команда /create работает корректно

## 🎉 Готово!

Ваш бот теперь работает 24/7 на Railway!

---

Нужна помощь? Проверьте [документацию Railway](https://docs.railway.app/)
