# Changelog: Добавление функции "Несколько городов"

## Дата: 17 февраля 2026

## Описание изменений

Реализована функциональность для создания дисклеймеров для нескольких городов одновременно.

## Что добавлено

### 1. Выбор сценария при создании дисклеймера

При запуске `/create` пользователь теперь видит два варианта:
- **🏙 Один город** — существующий режим (создание для одного города)
- **🌍 Несколько городов** — новый режим (создание для нескольких городов)

### 2. Множественный выбор городов

В режиме "Несколько городов":
- Пользователь может выбрать любое количество городов
- Выбранные города отмечаются галочкой ✅
- Кнопка "Все города" работает как переключатель (выбрать все / снять все)
- Кнопка "Далее" показывает количество выбранных городов

### 3. Генерация .txt файла

После заполнения параметров бот генерирует текстовый файл:
- **Формат файла**: чистый текст с разделителями между городами
- **Имя файла**: автоматически формируется на основе типа креатива
  - Примеры: `disclaimers_промокод.txt`, `disclaimers_скидка_новичка_динамическая.txt`
- **Содержимое**: название города заглавными буквами, разделитель, дисклеймер

### Пример файла:

```
МОСКВА
----------------------------------------
До 31.12.24 скидка 20% на первый заказ от 1000 ₽. Не применяется к товарам...

МО
----------------------------------------
До 31.12.24 скидка 20% на первый заказ от 1000 ₽. Не применяется к товарам...

ТУЛА
----------------------------------------
До 31.12.24 скидка 20% на первый заказ от 1000 ₽. Не применяется к товарам...
```

## Технические изменения

### models.py
- Добавлено поле `cities: Optional[List[str]]` в `CreativeParams`
- Поле `city` теперь опциональное
- Добавлена валидация: либо `city`, либо `cities` (но не оба сразу)

### states.py
- Добавлено состояние `choosing_scenario` — выбор режима
- Добавлено состояние `choosing_multiple_geography` — выбор нескольких городов

### keyboards.py
- Добавлена функция `get_scenario_keyboard()` — клавиатура выбора сценария
- Добавлена функция `get_multiple_geography_keyboard()` — клавиатура с чекбоксами для выбора городов

### generator.py
- Добавлен метод `generate_multiple()` — генерация для нескольких городов
- Добавлен метод `format_multiple_to_file()` — форматирование в текстовый файл
- Обновлена валидация для поддержки `cities`

### handlers.py
- Изменена точка входа `cmd_create` — теперь показывает выбор сценария
- Добавлен обработчик `process_scenario_selection` — обработка выбора режима
- Модифицирован `process_type_selection` — роутинг по режиму (single/multiple)
- Добавлен обработчик `process_multiple_geography` — обработка выбора нескольких городов
- Модифицирован `generate_disclaimer` — генерация для обоих режимов
- Обновлен `show_confirmation` — корректное отображение географии для обоих режимов

## Обратная совместимость

✅ Все существующие функции работают без изменений:
- Одиночный режим работает точно так же, как раньше
- Все типы креативов поддерживаются
- Валидация не изменилась для одиночного режима

## Тестирование

Создан тестовый скрипт `test_implementation.py` для проверки:
- Одиночного режима
- Множественного режима
- Валидации параметров
- Генерации для всех 34 городов

**Результат**: ✅ Все тесты пройдены успешно

## Использование

### Пример 1: Создание для нескольких городов

1. `/create`
2. Выбрать "🌍 Несколько городов"
3. Выбрать тип креатива (например, "Промокод")
4. Выбрать города (например, Москва, МО, Тула)
5. Нажать "➡️ Далее"
6. Заполнить параметры (канал, дата, скидка и т.д.)
7. Подтвердить
8. Получить .txt файл с дисклеймерами

### Пример 2: Создание для всех городов

1. `/create`
2. Выбрать "🌍 Несколько городов"
3. Выбрать тип креатива (например, "Имиджевый")
4. Нажать "✅ Все города"
5. Нажать "➡️ Далее"
6. Заполнить параметры
7. Получить файл с 34 дисклеймерами

## Статистика изменений

- **Файлов изменено**: 5
- **Новых функций**: 5
- **Новых обработчиков**: 3
- **Строк кода добавлено**: ~250
- **Поддерживаемых городов**: 34

## Автор

План и реализация: Claude Sonnet 4.5

## Дата релиза

17 февраля 2026
